/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", // Scans files in src for Tailwind classes
    ],
    theme: {
      extend: {}, // You can customize themes here later if needed
    },
    plugins: [], // You can add Tailwind plugins here later if needed
  }